# macOS
.DS_Store

# Temporary files
*.swp
*.swo
*~
__pycache__/
*.py[cod]
.dual-graph/
/CLAUDE.md
/.claude/settings.local.json

# paneru rules daemon bytecode
config/paneru/__pycache__/
/.playwright-mcp

# Claude Code MCP secrets (real keys live in ~/.claude/.mcp-secrets, never here)
config/claude/mcp-secrets.sh
config/claude/.mcp-secrets

# Codex private render values (real values live in ~/.codex/.mcp-secrets)
config/codex/mcp-secrets.sh
config/codex/.mcp-secrets

# Private Kubernetes watcher inventories live under ~/.config, never in dots.
**/deploy-watch.resources
