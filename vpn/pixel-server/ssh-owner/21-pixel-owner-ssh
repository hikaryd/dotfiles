#!/data/data/com.termux/files/usr/bin/sh
# Owner shell only. Independent of Chrome and the display lease.
umask 077
termux-wake-lock
exec flock -n "$HOME/pixel-owner/runner.lock" python "$HOME/pixel-owner/supervisor.py"
