<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  LaunchAgent для компаньон-демона авто-привязки приложений к рядам paneru.

  Скрипт запускается через login shell, чтобы путь строился от $HOME без
  машинно-зависимого абсолютного имени пользователя.

  Активация (config-only по умолчанию; этим симлинком агент НЕ грузится):
    launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.paneru.rules.plist
  Остановить/выгрузить:
    launchctl bootout   gui/$(id -u)/com.paneru.rules
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.paneru.rules</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/zsh</string>
        <string>-lc</string>
        <string>exec /usr/bin/python3 "$HOME/.config/paneru/paneru-rules.py"</string>
    </array>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    </dict>

    <key>StandardOutPath</key>
    <string>/tmp/paneru-rules.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/paneru-rules.log</string>
</dict>
</plist>
