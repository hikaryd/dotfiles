-- Search is handled by fff.nvim. Keep Telescope disabled so project file/content
-- search does not silently fall back to the old picker.
return {
	"nvim-telescope/telescope.nvim",
	enabled = false,
}
